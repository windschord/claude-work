# ClaudeWork 環境変数設定
# このファイルを /etc/claude-work/env にコピーして編集してください

# データベースパス（必須）
# SQLite データベースファイルのパスを指定します
DATABASE_URL=file:/opt/claude-work/data/claudework.db

# サーバーホスト（デフォルト: localhost）
# 外部からのアクセスを許可する場合は 0.0.0.0 を設定
# HOST=0.0.0.0

# サーバーポート（デフォルト: 3000）
# ClaudeWork が待ち受けるポート番号
PORT=3000

# ログレベル（debug, info, warn, error）
# 運用環境では info または warn を推奨
LOG_LEVEL=info

# Node.js 環境
# 本番環境では必ず production を設定
NODE_ENV=production

# プロセスのアイドルタイムアウト時間（分）（オプション）
# 一定時間操作がない Claude Code プロセスを自動的にサスペンドします（デフォルト: 30）
# PROCESS_IDLE_TIMEOUT_MINUTES=30

# Claude Code CLI パス（オプション）
# systemd サービスの PATH に /opt/claude-work/.local/bin が含まれているため、
# claude-work ユーザーにインストールした CLI は自動検出されます。
# 別の場所にインストールした場合は、絶対パスまたは PATH 上のコマンド名を指定してください。
# CLAUDE_CODE_PATH=/usr/local/bin/claude

# 許可するプロジェクトディレクトリ（オプション）
# カンマ区切りで複数指定可能
# ALLOWED_PROJECT_DIRS=/home/projects,/opt/projects

# CORS で許可するオリジン（オプションだがブラウザアクセス時は推奨）
# カンマ区切りで複数指定可能
# 本番環境では "*" は使用せず、必要なドメインのみを列挙してください
# 例: ALLOWED_ORIGINS=https://example.com,https://app.example.com
# ALLOWED_ORIGINS=

# 認証トークン（オプション）
# 設定すると API アクセス時にトークン認証が必要になります
# 安全なランダム文字列を生成してください（例: openssl rand -hex 32）
# CLAUDE_WORK_TOKEN=

# セッションシークレット（オプション）
# 設定する場合は 32 文字以上の安全なランダム文字列を使用してください
# SESSION_SECRET=
